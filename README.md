# flashcards

Simple flashcard TypeScript application using Preact and Vite. Data exists entirely in local storage, sharable via base64 encoded string -- with future plans to add serverless persistence.

Currently deployed to Cloudflare Pages at [flashcards.fyi](https://flashcards.fyi).

```
npm install
npm run dev
```
